/* 11576 */

#include <cstdio>
#include <cmath>
#include <vector>

using namespace std;

int main() {
  int A, B; scanf("%d %d", &A, &B);
  int m; scanf("%d", &m);
  int in = new int[m];
  for (int i = 0; i < m; i++) {
    scanf("%d", &in[i]);
  }

  int res = new int[m];
  int dec = 0;
  for (int i = 0; i < m; i++) {

  }


  return 0;
}
/*
17 8
2
2 16

6 2
*/